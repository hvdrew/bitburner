{
  "scripts": {
    "dev": "nodemon --watch src -e ts --exec npm run sync",
    "sync": "tsc -p tsconfig.json && bitburner-sync",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf build/*"
  },
  "dependencies": {
    "bitburner-sync": "^1.5.2",
    "nodemon": "^2.0.19"
  }
}
